<%= form_with(model: avaible_hour, local: true) do |form| %>
  <% if avaible_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avaible_hour.errors.count, "error") %> prohibited this avaible_hour from being saved:</h2>

      <ul>
      <% avaible_hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
